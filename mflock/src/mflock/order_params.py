"""Candidate order parameters for discourse-memory.

Filled in step 4. Three candidates, each a pure function over a conversation
trace: polarization (flocking magnetization), retention(d) (birth-death
decay), and susceptibility (linear response).
"""
